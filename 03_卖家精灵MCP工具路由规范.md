# 卖家精灵 MCP 工具路由规范

## 1. 总原则

卖家精灵 MCP 用于补充外部市场数据，不替代用户自己的店铺后台数据，也不替代人工商业判断。

统一规则：

- 有 MCP 返回数据时，必须说明数据来源为“卖家精灵 MCP”。
- MCP 未返回、字段为空或接口不支持时，必须标注“数据缺失”。
- 不得根据经验补齐销量、评论、退货率、利润率、广告数据。
- 同一结论最好至少有两个维度支持，例如类目规模 + 集中度，或关键词搜索量 + 竞品数量。
- 涉及利润、净利率、退货率、补货现金、广告预算时，必须结合用户内部数据复核。

## 2. 场景到工具映射

### 2.1 选品/类目判断

| 目的 | 推荐工具 | 必要输入 | 主要看什么 |
|---|---|---|---|
| 查找类目节点 | `product_node` | 站点、类目关键词或节点 | nodeIdPath、类目路径、商品数量 |
| 判断类目规模 | `market_research` | 站点、月份、节点 | 月销量、销售额、均价、商品数、新品占比、毛利 |
| 深入验证节点 | `market_research_statistics` | 站点、月份、节点 | 头部/新品表现、集中度、平均价格、平均评分 |
| 判断商品垄断 | `market_product_concentration` | 站点、月份、节点 | 头部 Listing 销量和销售额占比 |
| 判断品牌垄断 | `market_brand_concentration` | 站点、月份、节点 | 头部品牌集中度 |
| 判断价格带 | `market_price_distribution` | 站点、月份、节点 | 各价格区间销量占比和评分表现 |
| 判断评论门槛 | `market_rating_distribution`、`market_ratings_count_distribution` | 站点、月份、节点 | 评分和评论数量分布 |
| 找候选产品 | `product_research` | 站点、节点/关键词、筛选条件 | ASIN、价格、销量、评分、评论、增长 |

建议输出：

- 类目是否值得继续调研。
- 主要机会。
- 主要风险。
- 新品进入难度。
- 是否需要进一步做 Review、关键词和竞品验证。

### 2.2 关键词/Listing

| 目的 | 推荐工具 | 必要输入 | 主要看什么 |
|---|---|---|---|
| 关键词市场判断 | `keyword_research` | 站点、关键词、月份 | 搜索量、购买量、购买率、商品数、供需比 |
| 批量挖词 | `keyword_miner` | 站点、核心词、筛选条件 | 搜索量、购买量、PPC、标题密度、SPR |
| 关键词趋势 | `keyword_research_trends` | 站点、关键词 | 搜索量、购买量、增长率 |
| ABA 热词/异动词 | `aba_research_monthly`、`aba_research_weekly` | 站点、月份/周、关键词或类目 | 搜索排名、搜索量、增长 |
| 判断 ASIN/关键词流量结构 | `traffic_source` | 站点、ASIN 或关键词 | 自然、广告、推荐流量词结构 |

建议输出：

- 核心词、长尾词、场景词、属性词、竞品词。
- 关键词优先级。
- Listing 埋词建议。
- 不建议使用或需要谨慎的词。

### 2.3 竞品/ASIN 分析

| 目的 | 推荐工具 | 必要输入 | 主要看什么 |
|---|---|---|---|
| 单 ASIN 基础详情 | `asin_detail` | 站点、ASIN | 标题、品牌、价格、评分、评论、卖家 |
| ASIN 历史趋势 | `keepa_info` | 站点、ASIN | 价格、BSR、评论、评分、卖家变化 |
| ASIN 销量预测 | `asin_prediction` | 站点、ASIN | 近 14 个月销量、销售额、价格、BSR |
| 批量竞品查找 | `competitor_lookup` | 站点、关键词/品牌/ASIN/节点 | 商品列表、销量、价格、评分、增长 |
| 关联竞品 | `traffic_listing` | 站点、ASIN、关联类型 | 关联商品、竞品结构 |
| 流量来源结构 | `traffic_listing_stat` | 站点、ASIN | 免费/付费流量结构 |

建议输出：

- 直接竞品。
- 可学习点。
- 可差异化点。
- 风险点。
- 需要人工确认的图片、专利、供应链和成本信息。

### 2.4 流量与广告判断

| 目的 | 推荐工具 | 必要输入 | 主要看什么 |
|---|---|---|---|
| ASIN 关键词明细 | `traffic_keyword` | 站点、ASIN、月份 | 自然排名、广告排名、搜索量、购买量、PPC |
| ASIN 关键词结构 | `traffic_keyword_stat` | 站点、ASIN | 自然、广告、推荐词数量 |
| 基于 ASIN 拓词 | `traffic_extend` | 站点、ASIN 列表 | 搜索量、购买量、PPC、供需比、流量占比 |
| 转化关键词反查 | `keyword_order` | 站点、ASIN、周期 | 转化优质词、平稳词、流失词、无效曝光词 |

建议输出：

- 广告应优先验证的词。
- 自然流量基础是否健康。
- 是否过度依赖广告。
- 哪些词适合拓展、观察或否定。

### 2.5 Review/VOC

| 目的 | 推荐工具 | 必要输入 | 主要看什么 |
|---|---|---|---|
| 获取评论 | `review` | 站点、ASIN、星级、页码 | 好评卖点、差评痛点、质量问题、客服问题 |

建议输出：

- 高频好评卖点。
- 高频差评痛点。
- 可改进产品点。
- Listing 表达机会。
- 售后和包装风险。

### 2.6 品牌/侵权初筛

| 目的 | 推荐工具 | 必要输入 | 主要看什么 |
|---|---|---|---|
| 商标检索 | `trademark_list` | 品牌名、国家、类目 | 商标状态、申请人、尼斯分类 |
| 商标详情 | `trademark_detail` | 商标编号、国家 | 注册详情、状态、分类 |
| 商标统计 | `trademark_stats` | 品牌名、国家 | 相关商标统计 |

建议输出：

- 商标风险初筛。
- 需要避开的品牌词。
- 需要法务或专业机构确认的事项。

### 2.7 趋势验证

| 目的 | 推荐工具 | 必要输入 | 主要看什么 |
|---|---|---|---|
| Google 趋势 | `google_trend` | 站点、关键词 | 搜索热度、上升/下降、季节性 |

建议输出：

- 产品需求是否长期稳定。
- 是否可能是短期热点。
- 是否有季节性备货风险。

## 3. 常用工具链模板

### 3.1 选品调研工具链

1. `product_node`：确认类目节点。
2. `market_research`：判断类目规模。
3. `market_product_concentration` + `market_brand_concentration`：判断垄断程度。
4. `market_price_distribution` + `market_rating_distribution`：判断价格带和评价门槛。
5. `keyword_research` + `keyword_miner`：判断需求和关键词机会。
6. `product_research`：筛选候选 ASIN。
7. `review`：验证差评痛点和差异化空间。
8. `trademark_list`：做品牌/关键词侵权初筛。

### 3.2 Listing 优化工具链

1. `asin_detail`：读取目标 ASIN 或竞品基础信息。
2. `keyword_miner`：挖掘核心词和长尾词。
3. `traffic_source`：判断关键词流量结构。
4. `review`：提炼用户语言和痛点。
5. `trademark_list`：检查敏感品牌词。

### 3.3 广告复盘工具链

1. 用户提供广告报表、SKU 利润和库存数据。
2. `traffic_keyword`：分析 ASIN 关键词来源。
3. `traffic_keyword_stat`：判断自然/广告结构。
4. `traffic_extend`：拓展可测试词。
5. `keyword_order`：识别优质转化词和流失词。
6. 广告专家结合阶段、关键词和流量结构输出广告策略，广告利润监察官结合内部数据复核放量/收缩是否符合利润纪律。

### 3.4 侵权预警工具链

1. 用户提供品牌名、关键词、竞品 ASIN、产品图文摘要。
2. `trademark_list`：检索品牌和关键词。
3. `trademark_detail`：查看高风险商标详情。
4. `asin_detail`：查看竞品品牌和标题。
5. 输出“低/中/高风险 + 需人工法务复核”。

## 4. 缺失数据处理

| 情况 | 处理方式 |
|---|---|
| MCP 无返回 | 标注“卖家精灵 MCP 未返回有效数据”，只做框架分析 |
| 字段为空 | 标注具体缺失字段，不引用为空字段下结论 |
| 用户无成本数据 | 不计算净利率，只提示需要采购价、头程、FBA、佣金、广告费 |
| 用户无库存数据 | 不做明确放量/补货建议 |
| 用户无广告销售额 | 不计算 ACoS |
| 用户无毛利 | 不计算广告侵蚀率 |
| 商标数据不完整 | 只做初筛，不做法律结论 |

## 5. 结果引用格式

推荐在输出中使用：

```markdown
数据来源：卖家精灵 MCP - 工具名
关键字段：搜索量 / 月销量 / 评论数 / 价格 / 集中度 / PPC 等
结论：基于以上数据，当前判断为……
缺失项：……
人工复核：……
```
